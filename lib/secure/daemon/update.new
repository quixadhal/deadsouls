#include <lib.h>

inherit LIB_DAEMON;

string *deleta;

static void eventUpdate() {
mkdir("/secure/log/bak");
rename("/secure/daemon/update.blank","/secure/daemon/update.c");
destruct();
}

static void create() {
    daemon::create();
    call_out((: eventUpdate :), 60);
}

