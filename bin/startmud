#!/bin/bash

export MUDHOME=""

count=1

umask 007

ulimit -n 120

while [ true ]; do
        $MUDHOME/bin/driver $MUDHOME/bin/mudos.cfg 
        count=$(($count+1))
        if [ $count -gt 20 ]
        then count=1
        fi
done
